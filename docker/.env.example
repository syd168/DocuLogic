# DocuLogic — Docker Compose 环境文件说明
#
# 权威配置在「项目根目录」的 .env（由 .env.example 复制而来）。
# 运行 ./docker/deploy.sh 时会自动把根目录 .env 同步为本文件 docker/.env，
# 以便在本目录执行 docker compose 时能正确替换 ${HOST_PORT}、${DATA_DIR} 等。
#
# 手动编排时二选一：
#   1) 在项目根执行: ./docker/deploy.sh
#   2) 复制根目录 .env 到此处后: docker compose up -d --build
#
# cp ../.env .env
#
# 可选构建参数（写入 .env 或导出环境变量）:
#   INSTALL_MARKER=0   # 设为 1 时镜像内安装 marker-pdf（GPL）
#   PIP_INDEX=https://mirrors.huaweicloud.com/repository/pypi/simple
